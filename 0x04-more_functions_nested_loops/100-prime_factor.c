#include <stdio.h>
#include "main.h"

/**
 * main - entry point
 *
 * Description: whether char is upper or lower case
 *
 * Return: 1 (upper) 0 (lower)
*/

int main(void)
{
	int long i;

	for (i = 782849 ; ; i--)
	{
		if (612852475143 % i != 0)
		{
			printf("%ld\n", i);
			break;
		}
	}
	return (0);
}
