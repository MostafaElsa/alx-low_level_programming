#include "main.h"

/**
 *	_sqrt_recursion - check the code
 *	@n: number
 *	Return: result
 */
int _sqrt_recursion(int n)
{
	if (y < 0)
		return (-1);
	else if (y == 0)
		return (1);
	{
		y--;
		return (x * _pow_recursion(x, y));
	}
}
