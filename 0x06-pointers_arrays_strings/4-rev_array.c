#include <stdio.h>
#include "main.h"

/**
 * reverse_array - check the code
 *
 * @a: array
 * @n: number of elements
 * Return: Always 0.
 */

void reverse_array(int *a, int n)
{
	int j = 0;

	while (j < n / 2)
	{
		a[i] = a[n - 1 - i];
		i++;
	}
}
