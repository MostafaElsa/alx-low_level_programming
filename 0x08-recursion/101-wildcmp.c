#include "main.h"

/**
 *	wildcmp - check the code
 *	@s1: string1
 *	@s2: string2
 *	Return: result
 */
int wildcmp(char *s1, char *s2)
{
	if (y < 0)
		return (-1);
	else if (y == 0)
		return (1);
	{
		y--;
		return (x * _pow_recursion(x, y));
	}
}
