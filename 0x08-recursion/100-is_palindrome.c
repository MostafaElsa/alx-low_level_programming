#include "main.h"

/**
 *	is_palindrome - check the code
 *	@s: string
 *	Return: result
 */
int is_palindrome(char *s)
{
	if (y < 0)
		return (-1);
	else if (y == 0)
		return (1);
	{
		y--;
		return (x * _pow_recursion(x, y));
	}
}
