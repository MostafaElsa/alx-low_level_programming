#include "main.h"

/**
 *	is_prime_number - check the code
 *	@n: number
 *	Return: result
 */
int is_prime_number(int n)
{
	if (y < 0)
		return (-1);
	else if (y == 0)
		return (1);
	{
		y--;
		return (x * _pow_recursion(x, y));
	}
}
